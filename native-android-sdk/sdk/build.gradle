apply plugin: 'com.android.library'
apply plugin: 'kotlin-android'
apply plugin: 'maven-publish'

android {
    namespace 'com.flutterchat.sdk'
    compileSdk 34

    defaultConfig {
        minSdk 24
        targetSdk 34

        consumerProguardFiles 'consumer-rules.pro'
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = '17'
    }
}

dependencies {
    // Flutter module AAR — copied to libs/ by CI from 'flutter build aar' output
    implementation(name: 'flutter_release', ext: 'aar')

    // Flutter engine dependencies (consumers need storage.googleapis.com repo)
    implementation 'io.flutter:flutter_embedding_release:+'
    implementation 'io.flutter:armeabi_v7a_release:+'
    implementation 'io.flutter:arm64_v8a_release:+'
    implementation 'io.flutter:x86_64_release:+'

    implementation 'androidx.core:core-ktx:1.12.0'
    implementation 'androidx.appcompat:appcompat:1.6.1'
}

repositories {
    flatDir {
        dirs 'libs'
    }
}

def mavenRepoUrl = project.hasProperty('MAVEN_REPO_URL')
        ? project.property('MAVEN_REPO_URL')
        : "file://${buildDir}/maven-repo"

def sdkVersion = project.hasProperty('SDK_VERSION')
        ? project.property('SDK_VERSION')
        : '0.0.1-SNAPSHOT'

afterEvaluate {
    publishing {
        publications {
            release(MavenPublication) {
                from components.release

                groupId = 'com.flutterchat'
                artifactId = 'chat-sdk'
                version = sdkVersion

                pom {
                    name = 'Flutter Chat SDK'
                    description = 'A native Android SDK wrapping a Flutter chat module'
                }
            }
        }
        repositories {
            maven {
                url = mavenRepoUrl
            }
        }
    }
}
